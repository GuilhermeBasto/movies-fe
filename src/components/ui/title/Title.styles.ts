import styled from "styled-components";
import { colors } from "../../../theme/colors";

export const Title = styled.h6`
  font-family: "Roboto";
  font-size: 24px;
  font-weight: 400;
  margin: 0;
  color: ${colors.blue};
  letter-spacing: 0px;
`;
